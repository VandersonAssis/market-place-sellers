spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: default
            name: market-place-sellers
      reload:
        enabled: true
        mode: event
        strategy: refresh